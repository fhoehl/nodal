var document = this.window.document,
    location = this.window.location,
    navigator = this.window.navigator,
    nodal = {
        model: {},
        view: {},
        layout: {},
        loader: {}
    };
    
exports.nodal = nodal;
